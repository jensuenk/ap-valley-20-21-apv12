import { NewEnTimeModalPipe } from './new-en-time-modal.pipe';

describe('NewEnTimeModalPipe', () => {
  it('create an instance', () => {
    const pipe = new NewEnTimeModalPipe();
    expect(pipe).toBeTruthy();
  });
});
